import { MetresToKmPipe } from './metres-to-km.pipe';

describe('MetresToKmPipe', () => {
  it('create an instance', () => {
    const pipe = new MetresToKmPipe();
    expect(pipe).toBeTruthy();
  });
});
