import { SpaceAfterCharPipe } from './space-after-char.pipe';

describe('SpaceAfterCharPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceAfterCharPipe();
    expect(pipe).toBeTruthy();
  });
});
